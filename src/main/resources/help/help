Kastor Local Help

syntax
 $ kastor (directory) (port) -d

example
 $ kastor . -d

arguments
 directory  the directory to serve as webroot (default is cwd)
 port       the port to allocate (default is 7069)

flags
 -d  print debug information in the console